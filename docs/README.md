# WeekBar update feed

This directory is published with GitHub Pages.

The signed `appcast.xml` will be added by the WeekBar release process. Do not edit a
signed appcast manually; regenerate it with Sparkle's `generate_appcast` tool.

